import 'package:flutter/material.dart';

class ExampleLoadingView extends StatelessWidget {
  const ExampleLoadingView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
